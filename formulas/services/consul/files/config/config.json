{
    "server": true,
    "ports": {
      "http": -1,
      "https": 8501
    },
    "cert_file": "{{tls_cert_file}}",
    "key_file": "{{tls_key_file}}",
    "bootstrap_expect": 1,
    "ui": true,
    "bind_addr": "{{bind_addr}}",
    "data_dir": "{{data_dir}}",
    "enable_debug": true,
    "log_level": "{{log_level}}"
  }